name: extrapolations.dev
channels:
  - conda-forge
dependencies:
  - python=3.7
  - pip

  # Base conda deps
  - setuptools
  - setuptools_scm
  - pylint
  - flake8
  - black
  - isort
  - twine
  - wheel
  - pytest
  - pytest-cov

  # Base conda deps: NBconvert
  - pyyaml
  - ipython
  - nbconvert

  # Base conda deps: Notebooks
  - notebook
  - numpy
  - pandas
  - scipy
  - matplotlib
  - plotly
  - bokeh
