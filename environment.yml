name: extrapolations.dev
channels:
  - conda-forge
dependencies:
  - python=3.8
  - pip

  # Base conda deps
  - pylint
  - flake8
  - black
  - isort

  # Base conda deps: NBconvert
  - pyyaml
  - ipython
  - nbconvert

  # Base conda deps: Notebooks
  - notebook
  - numpy
  - pandas
  - scipy
  - matplotlib
  - plotly
  - bokeh
